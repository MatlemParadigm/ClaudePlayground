---
name: spec
description: Turn a rough idea into a written spec with acceptance criteria. Use when the user types /spec or says "spec this out".
---

Use the penny agent to turn this idea into a spec: $ARGUMENTS

When Penny returns, save the spec to specs/<short-kebab-name>.md and show
the user only the ambiguity list, so they can answer it quickly.
